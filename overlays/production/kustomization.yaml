namePrefix: production-
commonLabels:
  variant: production
bases:
  - ../../base
patchesStrategicMerge:
  - deployment.yaml
  - config-map.yaml
images:
  - name: ghcr.io/reachnaveen/springboot-app
    newTag: production-1  # Set dynamically from Jenkins
