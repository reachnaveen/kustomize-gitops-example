namePrefix: qa-
commonLabels:
  variant: qa
bases:
  - ../../base
patchesStrategicMerge:
  - deployment.yaml
  - config-map.yaml
images:
  - name: reachnaveen/springboot-app
    newTag: "qa-11" # Set dynamically from Jenkins
