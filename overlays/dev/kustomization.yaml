namePrefix: dev-
commonLabels:
  variant: dev
bases:
  - ../../base
patchesStrategicMerge:
  - config-map.yaml
images:
  - name: reachnaveen/springboot-app
    newTag: dev-11 # Set dynamically from Jenkins
