namePrefix: dev-
commonLabels:
  variant: dev
resources:
  - ../../base
patchesStrategicMerge:
  - deployment.yaml
configMapGenerator:
  - name: springboot-app-config
    files:
      - application.properties
generatorOptions:
  disableNameSuffixHash: true
images:
  - name: reachnaveen/springboot-app
    newTag: dev-17 # Set dynamically from CI
